<%= form_for(@grocery) do |f| %>
  <% if @grocery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grocery.errors.count, "error") %> prohibited this wiki from being saved:</h2>

      <ul>
      <% @grocery.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :item %><br />
  </div>
  <div class="submit">
    <%= f.submit @grocery.new_record? ? "Add New Grocery List Item" : "Update Grocery List Item" %>
  </div>
<% end %>

<%= link_to 'Return to Full List of Grocery Items', groceries_path, :class => "back-home" %>

